@using System.Text.Json
@{
    ViewData["Title"] = "Home Page";
}

<pre>
    @Html.Raw(JsonSerializer.Serialize(JsonDocument.Parse(ViewBag.JsonData), new JsonSerializerOptions { WriteIndented = true }))
</pre>